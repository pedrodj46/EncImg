<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="encimg.app"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"></uses-permission>

    <application
        android:name="VariabiliGlobali"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="encimg.app.Home"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="encimg.app.UserPage"
            android:label="@string/app_name" >
            <!--<meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="encimg.app.Home" />-->
        </activity>
        <activity
            android:name="encimg.app.Scelta"
            android:label="@string/app_name" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="encimg.app.UserPage" />
        </activity>
        <activity
            android:name="encimg.app.Visualizza"
            android:label="@string/app_name" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="encimg.app.UserPage" />
        </activity>
        <activity
            android:name="encimg.app.Utente"
            android:label="@string/app_name" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="encimg.app.UserPage" />
        </activity>
        <activity
            android:name="encimg.app.Impostazioni"
            android:label="@string/app_name" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="encimg.app.UserPage" />
        </activity>
    </application>

</manifest>